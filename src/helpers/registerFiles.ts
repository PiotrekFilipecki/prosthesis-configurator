export function registerBinaryFiles(): void {
  // No-op in the web app. The legacy helper relied on Node fs and is not used.
}

export function registerAFMFonts(): void {
  // No-op in the web app. The legacy helper relied on dNode fs and is not used.
}
